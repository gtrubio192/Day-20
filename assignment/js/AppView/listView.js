var ListItemView = Backbone.View.extend({

	initialize: function() {

		_.bindAll(
			this,
			'onCheckBoxClicked'
			'onModelChanged'
			);

		var listItemTemplate  = _.template($('template').html());
		this.$el = 


	}

})